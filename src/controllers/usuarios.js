// const Usuario = require("../models/usuarios");

// module.exports = app => {
//     app.get("/usuarios/nome", (req, res) => {
//         const nome = parse(req.params.nome);

//         Usuario.buscaPorNome(nome, res);
//     });
// };